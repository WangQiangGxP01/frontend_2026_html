import "./styles.css";

export default function App() {
  /**
   * チャレンジ：
   *
   * 1. `App` コンポーネントの中で'favoriteFood'（好きな食べ物）という変数を作成する
   * 2. 作成した変数を使って、以下の<p></p>を修正し、画面に表示する
   *    例: 好きな食べ物： ○○　　<--(変数)
   * 3. インラインCSSを追加する、文字の大きさと色を変えてみよう!
   *
   */
  const favoriteFood = "○○";

  return (
    <div className="App">
      <p></p>
    </div>
  );
}
